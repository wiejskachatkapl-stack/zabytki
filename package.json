{
  "name": "mapa-atrakcji-polski-android",
  "version": "1.0.90",
  "private": true,
  "description": "Mapa atrakcji Polski - Android (Capacitor)",
  "scripts": {
    "android:prepare": "npm install && node scripts/prepare-android.js",
    "android:sync": "npx cap sync android && node scripts/patch-android.js",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
